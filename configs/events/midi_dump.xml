<app name="midi_test">
	<variable name="connection" default="">Name of midi device to connect to</variable>

	<description>
		Dump all events from a midi device.
	</description>

	<node class="midi_device" name="midi">
		<parameter name="connection">@connection</parameter>
	</node>

	<node class="event_info" name="info"/>
	<event>

		route(double(midi:control_0_2, 0.0, 1.0))->info:xxx;

		route(select(gt(midi:control_0_2, midi:control_0_3),[True, "WTF"]))->info:y;

		route(
			select(
				gt(
					double(midi:control_0_5, 0.0, 1.0),
					0.5),
				[BANG, Null])
		)->info:stop,midi_test:stop;

		route(select(gt(midi:control_0_46,100),[BANG,NULL]))->midi_test:stop;
	</event>
</app>
